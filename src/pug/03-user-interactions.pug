doctype html
html(lang=en)
  head
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title 03. User Interaction
  body
    script.
      let userName;
      do {
        userName = prompt("Hello! What is your name?", "")
      } while(!userName)
      alert( `Your name is: ${userName}` )
